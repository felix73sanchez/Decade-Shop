import {Montserrat_Alternates, Exo_2} from "next/font/google";
import { Size } from '../interfaces/product.interface';

export const logoFont = Exo_2({ subsets: ["latin"], style: ["italic"], weight: ['900'], });

export const allFont = Exo_2({ subsets: ["latin"], weight: ['500', '700'], });

